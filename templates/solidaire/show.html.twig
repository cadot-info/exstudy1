{% extends 'base.html.twig' %}

{% block title %}Solidaire{% endblock %}

{% block body %}
    <h1>Solidaire</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ solidaire.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ solidaire.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ solidaire.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ solidaire.email }}</td>
            </tr>
            <tr>
                <th>Codepostal</th>
                <td>{{ solidaire.codepostal }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('solidaire_index') }}">back to list</a>

    <a href="{{ path('solidaire_edit', {'id': solidaire.id}) }}">edit</a>

    {{ include('solidaire/_delete_form.html.twig') }}
{% endblock %}
